# Icons inbox

Paste raw SVG below. One block per icon. Above each SVG, add a `name:` line
(kebab-case) and, if you know it, a `use:` line saying where it should go.
Don't worry about JSX formatting — paste the SVG exactly as copied.

Format:

    name: sales
    use: demo tab + feature card "pivot table"
    <svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" ...>
      ...
    </svg>

---

<!-- paste below this line -->
