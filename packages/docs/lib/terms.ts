export const terms: Record<string, string> = {
  "lifecycle":
    "The pattern of tracking how many consumers are using a resource so it can be cleaned up when nobody needs it.",
  "consumer-of-datasource":
    "Any code that reads data from a DataSource - for example, a pivot grid or a flat table.",
  "data-model":
    "The layer that takes raw data from a DataSource, transforms it (grouping, pivoting, aggregation), and produces a view model the renderer can display.",
};
